import { Meteor } from 'meteor/meteor';
import { Teams } from '../../models/server/models/Teams';
import { settings } from '../../settings';

// Meteor.startup(() => {
// 	settings.addGroup('Team', function() {
// 		this.add('Team_enabled', true, {
// 			group: 'Team',
// 			i18nLabel: 'Enable',
// 			type: 'boolean',
// 			public: true,
// 		});
// 	});
// });

